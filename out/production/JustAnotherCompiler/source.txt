extern int foo2(int x);
int foo(int count)
{
 int sum = 0;
 for (int i = 1; i <= count; i++)
 sum += foo2(i);
 return sum;
}

#include <stdio.h>
#define min(X, Y)  ((X) < (Y) ? (X) : (Y))
main()
{
/* comment 1 */
   return min (1,2);
}

